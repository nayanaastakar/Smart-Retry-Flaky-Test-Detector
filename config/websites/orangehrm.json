{
  "website_name": "OrangeHRM",
  "base_url": "https://opensource-demo.orangehrmlive.com/",
  "credentials": {
    "username": "Admin",
    "password": "admin123"
  },
  "locators": {
    "login": {
      "username_field": ["name", "username"],
      "password_field": ["name", "password"],
      "login_button": ["css selector", "button[type='submit']"]
    },
    "inventory": {
      "inventory_container": ["class name", "oxd-layout"],
      "first_add_to_cart": ["xpath", "//a[contains(@href, 'viewPimModule')]"],
      "cart_badge": ["css selector", ".oxd-main-menu-item.active"],
      "cart_icon": ["class name", "oxd-userdropdown-name"],
      "menu_btn": ["class name", "oxd-userdropdown-tab"],
      "logout_link": ["xpath", "//a[text()='Logout']"]
    },
    "cart": {
      "cart_list": ["class name", "orangehrm-container"],
      "checkout_button": ["class name", "oxd-button--secondary"],
      "remove_button": ["class name", "oxd-icon-button--danger"]
    }
  },
  "settings": {
    "max_retries": 3,
    "timeout": 20
  }
}
